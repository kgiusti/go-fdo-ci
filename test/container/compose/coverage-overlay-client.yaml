services:
  go-fdo-client:
    build:
      context: ${client_src_dir}
      dockerfile: ${coverage_dockerfile_client}
      args:
        GOFLAGS: "-cover -covermode=atomic"
    environment:
      GOCOVERDIR: ${container_working_dir:-/workdir}/coverage
    volumes:
      - ${client_coverage_dir}:${container_working_dir:-/workdir}/coverage:z
